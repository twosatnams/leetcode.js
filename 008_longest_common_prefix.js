var longestCommonPrefix = function(array) {
    var prefix = true;
    var length = 0;
    var previousLetter = array[0][0];
    while (prefix) {
        for (var i = 0; i < array.length; i++) {
            var string = array[i];
            var letter = string[length];
            if (letter === )
        }
    }
};
